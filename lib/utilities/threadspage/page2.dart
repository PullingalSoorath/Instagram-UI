import 'package:flutter/material.dart';

class ThreadsPage2 extends StatelessWidget {
  const ThreadsPage2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('page 2',
      
      
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}